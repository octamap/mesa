
`A build-time HTML component engine that allows ${``} you to write reusable, declarative HTML components
with scoped CSS and
            dynamic attributes — <span class="bold">without requiring runtime JavaScript to render them.</span>`


